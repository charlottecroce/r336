// E106 — the same lemma exported by two modules.
//
// Verbs join the vocabulary unqualified, so there's no way to tell which
// `fógair` is meant. Rename one.
a seasmhach = ó "./E106-a.r336"
b seasmhach = ó "./E106-b.r336"
gníomh príomh() { fógair "x" }
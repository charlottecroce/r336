// E504 — an ongoing action called from one that isn't.
//
// Completing a command auto-awaits it, so the caller must be `ag` too.

ag gníomh sábháil(t: Teaghrán) {
    scríobh t
}

gníomh príomh() {
    sábháil "rud"
}